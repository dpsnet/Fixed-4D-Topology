% neurips_2026.sty
% Modified from neurips_2024.sty for NeurIPS 2026
% This file is released under the MIT license
% Original source: https://media.neurips.cc/

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{neurips_2026}[2026/01/01 NeurIPS 2026 submission/camera-ready style file]

% Preprint option
\newif\if@preprint
\@preprintfalse
\DeclareOption{preprint}{
  \@preprinttrue
}

\ProcessOptions\relax

% Use Times New Roman font
\RequirePackage{times}

% Page layout
\RequirePackage{geometry}
\geometry{
  letterpaper,
  textheight=9in,
  textwidth=5.5in,
  top=1in,
  headheight=12pt,
  headsep=25pt,
  footskip=30pt
}

% Line spacing
\RequirePackage{setspace}
\onehalfspacing

% Title formatting
\def\@maketitle{
  \vbox{
    \hsize\textwidth
    \linewidth\hsize
    \vskip 0.1in
    \centering
    {\LARGE\bfseries\@title\par}
    \vskip 0.2in
    {\def\and{\unskip\enspace{\rm and}\enspace}%
    \def\And{\end{tabular}\hss \egroup \hskip 1in plus 2fil 
           \hbox to 0pt \bgroup \hss \begin{tabular}[t]{c}}%
    \def\AND{\end{tabular}\hss\egroup \hfil\hfil\egroup
           \vskip 0.25in plus 1fil minus 0.125in
           \hbox to \linewidth\bgroup \hfil\hfil
           \hbox to 0pt\bgroup\hss\begin{tabular}[t]{c}}
    \hbox to \linewidth\bgroup \hfil\hfil
    \hbox to 0pt\bgroup\hss\begin{tabular}[t]{c}\@author 
                           \end{tabular}\hss\egroup
    \hfil\hfil\egroup}
    \vskip 0.3in
  }
}

% Abstract formatting
\renewenvironment{abstract}{
  \begin{center}
  \begin{minipage}{0.9\textwidth}
  \small
}{
  \end{minipage}
  \end{center}
  \vskip 0.2in
}

% Section formatting
\RequirePackage{titlesec}
\titleformat{\section}{\large\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection}{1em}{}
\titleformat{\subsubsection}{\normalsize\itshape}{\thesubsubsection}{1em}{}

% Bibliography style hint
\bibliographystyle{plainnat}

% Hyperref setup
\RequirePackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  citecolor=blue,
  urlcolor=blue,
  pdfstartview=FitH
}

% Remove page numbers for submission
\if@preprint
  % Keep page numbers for preprint
\else
  \pagestyle{empty}
\fi

% End of file
